# Natural Language to Command

I will translate your natural language request into a precise command using SENA's NL Query system.

**Examples:**
- "list all python files" → `find . -name "*.py"`
- "show git changes" → `git diff`
- "run tests" → `npm test` or `pytest`

**Usage:** /translate-nl <your natural language request>

Now translating to command:
